[project]
name = "sam3-vs-gs2"
version = "0.1.0"
description = "Orchestration scripts for SAM3 vs Grounded SAM 2 comparison"
requires-python = ">=3.12"
dependencies = [
    "pillow>=11.0",
]

[tool.uv]
package = false
